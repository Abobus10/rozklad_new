# 🎓 Telegram Бот для Расписания Колледжа

Современный Telegram бот для управления расписанием учебных занятий с типизацией, валидацией данных и структурированным логированием.

## ✨ Особенности

- 📅 **Расписание занятий** - просмотр расписания на день, неделю
- 🔔 **Уведомления** - персональные напоминания и уведомления о парах
- 👥 **Групповые чаты** - поддержка работы в групповых чатах
- 🎮 **Интерактивность** - клавиатуры, игры, диалоги
- 📊 **Статистика** - аналитика использования бота
- 🛡️ **Типизация** - полная типизация с Pydantic
- 📝 **Логирование** - структурированные логи с ротацией
- ⚙️ **Конфигурация** - гибкая настройка через переменные окружения

## 🏗️ Архитектура

### Структура проекта

```
бот расписание коледж/
├── models.py              # Pydantic модели данных
├── data_manager.py        # Управление данными с валидацией
├── config.py              # Конфигурация с Pydantic Settings
├── logger_config.py       # Настройка логирования
├── schedule_logic.py      # Бизнес-логика расписания
├── bot.py                 # Основной файл бота
├── notifications.py       # Система уведомлений
├── keyboards.py           # Telegram клавиатуры
├── handlers/              # Обработчики команд и событий
│   ├── commands.py
│   ├── callbacks.py
│   ├── conversations.py
│   └── utils.py
├── requirements.txt       # Зависимости
├── env_example.txt        # Пример конфигурации
└── README.md             # Документация
```

### Ключевые модули

#### 📦 `models.py`
- **UserModel** - модель пользователя с валидацией
- **LessonModel** - модель учебного занятия
- **ScheduleDataModel** - модель данных расписания
- **GroupChatModel** - модель группового чата
- **AppConfigModel** - модель конфигурации

#### 🗄️ `data_manager.py`
- **DataManager** - основной класс для работы с данными
- Thread-safe операции с файлами
- Автоматическая валидация при загрузке/сохранении
- Резервное копирование поврежденных файлов

#### ⚙️ `config.py`
- **AppConfig** - конфигурация с Pydantic Settings
- Загрузка из переменных окружения
- Валидация настроек при запуске
- Утилиты для работы с расписанием

#### 📊 `logger_config.py`
- **BotLogger** - настройка логирования
- Цветной вывод в консоль
- Ротация логов по размеру
- Структурированное логирование

#### 📅 `schedule_logic.py`
- **ScheduleService** - сервис для работы с расписанием
- Определение учебной недели
- Поиск следующих/текущих занятий
- Форматирование расписания

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd "бот расписание коледж"
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Скопируйте `env_example.txt` в `.env` и заполните необходимые значения:

```bash
cp env_example.txt .env
```

Отредактируйте `.env`:

```env
TELEGRAM_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
TIMEZONE=Europe/Kiev
LOG_LEVEL=INFO
```

### 4. Настройка расписания

Отредактируйте `schedule.json` согласно вашему расписанию:

```json
{
  "startDate": "2024-09-01",
  "groups": {
    "ГР-21": {
      "schedule": {
        "понеділок": [
          {
            "pair": 1,
            "name": "Математика",
            "teacher": "Иванов И.И.",
            "room": "101",
            "weeks": [1, 2]
          }
        ]
      }
    }
  }
}
```

### 5. Запуск бота

```bash
python bot.py
```

## 🛠️ Разработка

### Добавление новых команд

1. Создайте обработчик в `handlers/commands.py`:

```python
async def my_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработчик новой команды."""
    await update.message.reply_text("Привет!")
```

2. Зарегистрируйте команду в `bot.py`:

```python
self.application.add_handler(CommandHandler("mycommand", commands.my_command))
```

### Добавление новых моделей

Создайте модель в `models.py`:

```python
class MyModel(BaseModel):
    """Описание модели."""
    
    field: str = Field(description="Описание поля")
    
    @field_validator('field')
    @classmethod
    def validate_field(cls, v: str) -> str:
        """Валидация поля."""
        return v.strip()
```

### Логирование

Используйте логирование в своих модулях:

```python
from logger_config import get_module_logger

logger = get_module_logger(__name__)

def my_function():
    logger.info("Информационное сообщение")
    logger.error("Ошибка", exc_info=True)
```

## 📋 Команды бота

### Основные команды
- `/start` - Запуск бота
- `/help` - Справка
- `/menu` - Главное меню

### Расписание
- `/today` - Расписание на сегодня
- `/tomorrow` - Расписание на завтра
- `/week` - Расписание на неделю
- `/next` - Следующая пара
- `/schedule` - Полное расписание

### Настройки
- `/setgroup` - Установить группу
- `/reminders` - Настройка уведомлений
- `/me` - Мои настройки

### Администрирование
- `/admin` - Панель администратора
- `/stats` - Статистика бота
- `/broadcast` - Рассылка сообщений

## 🔧 Настройки

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_TOKEN` | Токен бота | - |
| `ADMIN_IDS` | ID администраторов | - |
| `TIMEZONE` | Часовая зона | Europe/Kiev |
| `LOG_LEVEL` | Уровень логирования | INFO |
| `LOG_FILE` | Файл логов | bot.log |
| `REQUEST_TIMEOUT` | Таймаут запросов | 30 |

### Файлы данных

- `users.json` - данные пользователей
- `schedule.json` - расписание занятий  
- `group_chats.json` - настройки групповых чатов

## 📊 Логирование

Бот ведет структурированные логи:

- **Консоль** - цветной вывод для разработки
- **Файл** - подробные логи с ротацией
- **Уровни** - DEBUG, INFO, WARNING, ERROR, CRITICAL

Логи автоматически ротируются при достижении 10MB.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👨‍💻 Автор

Создано для управления расписанием колледжа с использованием современных практик Python разработки.

---

**Примечание**: Этот проект демонстрирует использование современных подходов в Python разработке:
- Типизация с Pydantic
- Валидация данных
- Структурированное логирование
- Конфигурация через переменные окружения
- Thread-safe операции
- Graceful shutdown
- Документированный код 